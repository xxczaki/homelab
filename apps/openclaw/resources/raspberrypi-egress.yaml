apiVersion: v1
kind: Service
metadata:
  name: raspberrypi
  namespace: openclaw-system
  annotations:
    tailscale.com/tailnet-fqdn: raspberrypi.tail12a84.ts.net.
spec:
  type: ExternalName
  externalName: placeholder
  ports:
    - port: 22
      protocol: TCP
      name: ssh
